{
  "name": "team-task",
  "version": "1.0.0",
  "private": true,
  "description": "A monorepo for the team-task project",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd client && pnpm run dev",
    "build:client": "cd client && pnpm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Andre Harper",
  "license": "ISC",
  "devDependencies": {
    "typescript": "5.5.4"
  }
}

